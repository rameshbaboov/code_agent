import argparse
from math import sqrt as root   # Overriding the default 'sqrt' function provided by Python standard library because it is not a builtin or part of `math` module in python 3+ and we want to use its functionality instead, if available. If you have more complex mathematical operations which are required then they should be placed inside your own custom math functions
import operator as op    # For using the operators: + , - etc... directly without writing 'op.' before each function call like `add(a, b)` is equivalent to calling `operator['+']((a), (b))'  if you want an easier way. Here it just mimics python standard library functionality
import yaml   # To parse the metaspec YAML file into a Python dictionary with all its contents and properties using 'yaml'.load() method from pyyaml package in order to use safe expression evaluation feature provided by this script via --expr argument.  This allows user input of expressions directly through command line, which is easier than parsing it every time
import sys   # To handle exceptions if the yaml file does not exist or its contents are incorrect formatting and can't be parsed to Python dictionary like 'yaml'.load() method in pyyamlloader package. It provides a better user experience by providing informative messages about what went wrong while parsing YAML
import re   # For regex pattern matching used for safe expression evaluation feature, e.g., checking if the input string is an appropriate mathematical operation or not using 're' module in python 3+ which allows us to use regular expressions and match patterns with strings more easily than can be done by simply calling Python functions
import pytest   # For running unit tests (python files starting test_ prefix) via command line. It provides a simple yet powerful way of testing your code using the built-in feature that comes along with python unittest module for creating and managing complex functional requirements within small blocks called Test Cases in Python 
import pytest   # For running CLI tests (python files starting test_ prefix) via command line. It provides a simple yet powerful way of testing your code using the built-in feature that comes along with python unittest module for creating and managing complex functional requirements within small blocks called Test Cases in Python 
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
import sys   # To provide exception handling like if any error occurs during execution then it should be handled appropriately by providing informative messages to user, can include writing into a file or database etc. In this script that's done via command line argument '-q', '--quiet' mode for pytest which provides less noise and makes the output more readable
